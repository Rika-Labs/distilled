{
  "name": "@distilled.cloud/bench-bundle",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Rolldown bundle-size / tree-shake / bundle-time measurements for Distilled SDKs, using Alchemy's bundler settings.",
  "scripts": {
    "bench": "bun run.ts",
    "record": "bun run.ts --out results/latest.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@distilled.cloud/aws": "workspace:*",
    "@rikalabs/distilled-cloudflare": "workspace:*",
    "@distilled.cloud/core": "1.0.0-rc.9",
    "effect": "catalog:"
  },
  "devDependencies": {
    "@oxc-project/types": "catalog:",
    "@types/bun": "catalog:",
    "@types/node": "catalog:",
    "rolldown": "catalog:",
    "typescript": "catalog:"
  }
}
