// AUTO-GENERATED by scripts/generate.ts from .generated-specs (e2b openapi/envd/proto → smithy). Do not edit.
import * as S from "@rikalabs/distilled-core/schema";
import * as Redacted from "effect/Redacted";
import * as API from "@rikalabs/distilled-core/api";
import * as T from "../traits.ts";
import {
  E2BProtocol,
  type E2BOpError,
  type E2BOpContext,
} from "../protocol.ts";
import { UnknownE2BError } from "../errors.ts";
import * as Retry from "../retry.ts";

export type { E2BOpError, E2BOpContext };

export interface ConnectSandboxRequest {
  sandboxID: string;
  /** Timeout in seconds from the current time after which the sandbox should expire */
  timeout: number;
  /** Defaults to true. When false and the sandbox is paused, resume from disk state only: the sandbox cold-boots fresh and any memory in the snapshot is ignored, never modified or deleted. Disk state has crash-recovery semantics — writes not flushed before the pause may be lost. A no-op for snapshots that contain no memory. Rejected with an error in environments where this capability is not enabled, never silently downgraded to a memory restore. */
  memory?: boolean;
}
export const ConnectSandboxRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sandboxID: S.String.pipe(T.Label()),
    timeout: S.Number,
    memory: S.optional(S.Boolean),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/sandboxes/{sandboxID}/connect",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ConnectSandboxRequest",
}) as any as S.Schema<ConnectSandboxRequest>;

export interface Sandbox {
  /** Identifier of the template from which is the sandbox created */
  templateID: string;
  /** Identifier of the sandbox */
  sandboxID: string;
  /** Alias of the template */
  alias?: string;
  /** Identifier of the client */
  clientID: string;
  envdVersion: string;
  /** Access token used for envd communication */
  envdAccessToken?: string;
  /** Token required for accessing sandbox via proxy. */
  trafficAccessToken?: string | null;
  /** Base domain where the sandbox traffic is accessible */
  domain?: string | null;
}
export const Sandbox = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    templateID: S.String,
    sandboxID: S.String,
    alias: S.optional(S.String),
    clientID: S.String,
    envdVersion: S.String,
    envdAccessToken: S.optional(S.String),
    trafficAccessToken: S.optional(S.NullOr(S.String)),
    domain: S.optional(S.NullOr(S.String)),
  }),
).annotate({ identifier: "Sandbox" }) as any as S.Schema<Sandbox>;

/** Auto-resume configuration for paused sandboxes. */
export interface SandboxAutoResumeConfig {
  enabled: boolean;
}
export const SandboxAutoResumeConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    enabled: S.Boolean,
  }),
).annotate({
  identifier: "SandboxAutoResumeConfig",
}) as any as S.Schema<SandboxAutoResumeConfig>;

/** List of allowed destinations for egress traffic. Each entry can be a CIDR block (e.g. "8.8.8.8/32"), a bare IP address (e.g. "8.8.8.8"), or a domain name (e.g. "example.com", "*.example.com"). Allowed entries always take precedence over denied entries. */
export type SandboxNetworkConfigAllowOutList = Array<string>;
export const SandboxNetworkConfigAllowOutList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<SandboxNetworkConfigAllowOutList>;

/** List of denied CIDR blocks or IP addresses for egress traffic. Domain names are not supported for deny rules. */
export type SandboxNetworkConfigDenyOutList = Array<string>;
export const SandboxNetworkConfigDenyOutList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<SandboxNetworkConfigDenyOutList>;

/** SOCKS5 proxy for sandbox egress. Outbound TCP is tunneled through the proxy after allow/deny filtering; the sandbox is unaware. Domain-matched flows use remote DNS (ATYP=domain). */
export interface SandboxEgressProxyConfig {
  /** SOCKS5 proxy address in host:port format (e.g. "proxy.example.com:1080"). */
  address: string;
  /** Optional SOCKS5 username (RFC 1929), max 255 bytes. */
  username?: string;
  /** Optional SOCKS5 password (RFC 1929), max 255 bytes. */
  password?: string | Redacted.Redacted<string>;
}
export const SandboxEgressProxyConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    address: S.String,
    username: S.optional(S.String),
    password: S.optional(S.String.pipe(T.SensitiveValue({}))),
  }),
).annotate({
  identifier: "SandboxEgressProxyConfig",
}) as any as S.Schema<SandboxEgressProxyConfig>;

/** Sandbox ports that serve HTTPS rather than plaintext HTTP. Affects how the proxy reaches the service inside the sandbox; the public URL is HTTPS either way. Certificates are not verified, so self-signed ones work. The envd port (49983) cannot be listed. */
export type SandboxNetworkConfigHttpsPortsList = Array<number>;
export const SandboxNetworkConfigHttpsPortsList = /*@__PURE__*/ S.Array(
  S.Number,
) as any as S.Schema<SandboxNetworkConfigHttpsPortsList>;

/** HTTP headers to inject or override in matching requests. An existing header with the same name is replaced. Values are plain strings; secret resolution happens client-side before sending to the API. */
export type SandboxNetworkTransformHeadersMap = {
  [key: string]: string | undefined;
};
export const SandboxNetworkTransformHeadersMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<SandboxNetworkTransformHeadersMap>;

/** Transformations applied to matching egress requests before forwarding. */
export interface SandboxNetworkTransform {
  /** HTTP headers to inject or override in matching requests. An existing header with the same name is replaced. Values are plain strings; secret resolution happens client-side before sending to the API. */
  headers?: SandboxNetworkTransformHeadersMap;
}
export const SandboxNetworkTransform = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    headers: S.optional(SandboxNetworkTransformHeadersMap),
  }),
).annotate({
  identifier: "SandboxNetworkTransform",
}) as any as S.Schema<SandboxNetworkTransform>;

/** Transform rule applied to egress requests matching a domain pattern. */
export interface SandboxNetworkRule {
  transform?: SandboxNetworkTransform;
}
export const SandboxNetworkRule = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    transform: S.optional(SandboxNetworkTransform),
  }),
).annotate({
  identifier: "SandboxNetworkRule",
}) as any as S.Schema<SandboxNetworkRule>;

export type SandboxNetworkConfigRulesValueList = Array<SandboxNetworkRule>;
export const SandboxNetworkConfigRulesValueList = /*@__PURE__*/ S.Array(
  SandboxNetworkRule,
) as any as S.Schema<SandboxNetworkConfigRulesValueList>;

/** Per-domain transform rules applied to matching outbound HTTPS requests. Keys may be exact DNS names (for example, "api.example.com") or a leading wildcard (for example, "*.example.com"), and are normalized to lowercase on write. Wildcards match subdomains at any depth but not the apex domain; a bare "*" is invalid. Exact rules take precedence, followed by the longest matching wildcard suffix, and matching rule sets are not merged. Broad wildcards such as "*.com" are allowed and may expose transformed credentials to every matching destination the sandbox contacts. Rules do not grant network access; configure allowOut separately to permit the destination. */
export type SandboxNetworkConfigRulesMap = {
  [key: string]: SandboxNetworkConfigRulesValueList | undefined;
};
export const SandboxNetworkConfigRulesMap = /*@__PURE__*/ S.Record(
  S.String,
  SandboxNetworkConfigRulesValueList,
) as any as S.Schema<SandboxNetworkConfigRulesMap>;

export interface SandboxNetworkConfig {
  /** Specify if the sandbox URLs should be accessible only with authentication. */
  allowPublicTraffic?: boolean;
  /** List of allowed destinations for egress traffic. Each entry can be a CIDR block (e.g. "8.8.8.8/32"), a bare IP address (e.g. "8.8.8.8"), or a domain name (e.g. "example.com", "*.example.com"). Allowed entries always take precedence over denied entries. */
  allowOut?: SandboxNetworkConfigAllowOutList;
  /** List of denied CIDR blocks or IP addresses for egress traffic. Domain names are not supported for deny rules. */
  denyOut?: SandboxNetworkConfigDenyOutList;
  egressProxy?: SandboxEgressProxyConfig | null;
  /** Specify host mask which will be used for all sandbox requests */
  maskRequestHost?: string;
  /** Sandbox ports that serve HTTPS rather than plaintext HTTP. Affects how the proxy reaches the service inside the sandbox; the public URL is HTTPS either way. Certificates are not verified, so self-signed ones work. The envd port (49983) cannot be listed. */
  httpsPorts?: SandboxNetworkConfigHttpsPortsList;
  /** Per-domain transform rules applied to matching outbound HTTPS requests. Keys may be exact DNS names (for example, "api.example.com") or a leading wildcard (for example, "*.example.com"), and are normalized to lowercase on write. Wildcards match subdomains at any depth but not the apex domain; a bare "*" is invalid. Exact rules take precedence, followed by the longest matching wildcard suffix, and matching rule sets are not merged. Broad wildcards such as "*.com" are allowed and may expose transformed credentials to every matching destination the sandbox contacts. Rules do not grant network access; configure allowOut separately to permit the destination. */
  rules?: SandboxNetworkConfigRulesMap;
}
export const SandboxNetworkConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    allowPublicTraffic: S.optional(S.Boolean),
    allowOut: S.optional(SandboxNetworkConfigAllowOutList),
    denyOut: S.optional(SandboxNetworkConfigDenyOutList),
    egressProxy: S.optional(S.NullOr(SandboxEgressProxyConfig)),
    maskRequestHost: S.optional(S.String),
    httpsPorts: S.optional(SandboxNetworkConfigHttpsPortsList),
    rules: S.optional(SandboxNetworkConfigRulesMap),
  }),
).annotate({
  identifier: "SandboxNetworkConfig",
}) as any as S.Schema<SandboxNetworkConfig>;

export type SandboxMetadata = { [key: string]: string | undefined };
export const SandboxMetadata = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<SandboxMetadata>;

export type EnvVars = { [key: string]: string | undefined };
export const EnvVars = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<EnvVars>;

/** MCP configuration for the sandbox */
export type Mcp = { [key: string]: unknown | undefined };
export const Mcp = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<Mcp>;

export interface SandboxIamToken {
  /** Audience of the workload token, stored exactly as provided. */
  audience: string;
  /** Workload token type. */
  tokenType: string;
}
export const SandboxIamToken = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    audience: S.String,
    tokenType: S.String,
  }),
).annotate({
  identifier: "SandboxIamToken",
}) as any as S.Schema<SandboxIamToken>;

/** Named workload-token definitions, keyed by a caller-chosen token name. */
export type SandboxIamTokens = { [key: string]: SandboxIamToken | undefined };
export const SandboxIamTokens = /*@__PURE__*/ S.Record(
  S.String,
  SandboxIamToken,
) as any as S.Schema<SandboxIamTokens>;

/** Sandbox workload identity configuration. A non-empty, valid tokens map enables workload identity for the sandbox. */
export interface SandboxIam {
  tokens?: SandboxIamTokens;
}
export const SandboxIam = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    tokens: S.optional(SandboxIamTokens),
  }),
).annotate({ identifier: "SandboxIam" }) as any as S.Schema<SandboxIam>;

export interface SandboxVolumeMount {
  /** Name of the volume */
  name: string;
  /** Path of the volume */
  path: string;
}
export const SandboxVolumeMount = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String,
    path: S.String,
  }),
).annotate({
  identifier: "SandboxVolumeMount",
}) as any as S.Schema<SandboxVolumeMount>;

export type CreateSandboxRequestVolumeMountsList = Array<SandboxVolumeMount>;
export const CreateSandboxRequestVolumeMountsList = /*@__PURE__*/ S.Array(
  SandboxVolumeMount,
) as any as S.Schema<CreateSandboxRequestVolumeMountsList>;

export interface CreateSandboxRequest {
  /** Identifier of the required template */
  templateID: string;
  /** Time to live for the sandbox in seconds. */
  timeout?: number;
  /** Automatically pauses the sandbox after the timeout */
  autoPause?: boolean;
  /** Controls the snapshot kind taken when the sandbox auto-pauses on timeout (only relevant when autoPause is true). When false, the auto-pause drops the in-memory state and persists only the filesystem (a filesystem-only snapshot); resuming it cold-boots (reboots) the sandbox from disk. Such a snapshot cannot be auto-resumed by traffic and must be resumed explicitly, so it cannot be combined with autoResume. Defaults to true (full memory snapshot). */
  autoPauseMemory?: boolean;
  autoResume?: SandboxAutoResumeConfig;
  /** Secure all system communication with sandbox */
  secure?: boolean;
  /** Allow sandbox to access the internet. When set to false, it behaves the same as specifying denyOut to 0.0.0.0/0 in the network config. */
  allow_internet_access?: boolean;
  network?: SandboxNetworkConfig;
  metadata?: SandboxMetadata;
  envVars?: EnvVars;
  mcp?: Mcp | null;
  iam?: SandboxIam;
  volumeMounts?: CreateSandboxRequestVolumeMountsList;
}
export const CreateSandboxRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    templateID: S.String,
    timeout: S.optional(S.Number),
    autoPause: S.optional(S.Boolean),
    autoPauseMemory: S.optional(S.Boolean),
    autoResume: S.optional(SandboxAutoResumeConfig),
    secure: S.optional(S.Boolean),
    allow_internet_access: S.optional(S.Boolean),
    network: S.optional(SandboxNetworkConfig),
    metadata: S.optional(SandboxMetadata),
    envVars: S.optional(EnvVars),
    mcp: S.optional(S.NullOr(Mcp)),
    iam: S.optional(SandboxIam),
    volumeMounts: S.optional(CreateSandboxRequestVolumeMountsList),
  }).pipe(T.Http({ method: "POST", uri: "/sandboxes", code: 200 })),
).annotate({
  identifier: "CreateSandboxRequest",
}) as any as S.Schema<CreateSandboxRequest>;

export interface CreateSandboxRefreshRequest {
  sandboxID: string;
  /** Duration for which the sandbox should be kept alive in seconds */
  duration?: number;
}
export const CreateSandboxRefreshRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sandboxID: S.String.pipe(T.Label()),
    duration: S.optional(S.Number),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/sandboxes/{sandboxID}/refreshes",
      code: 200,
    }),
  ),
).annotate({
  identifier: "CreateSandboxRefreshRequest",
}) as any as S.Schema<CreateSandboxRefreshRequest>;

export interface CreateSandboxRefreshResponse {}
export const CreateSandboxRefreshResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "CreateSandboxRefreshResponse",
}) as any as S.Schema<CreateSandboxRefreshResponse>;

export interface CreateSandboxSnapshotRequest {
  sandboxID: string;
  /** Optional name for the snapshot template. If a snapshot template with this name already exists, a new build will be assigned to the existing template instead of creating a new one. */
  name?: string;
}
export const CreateSandboxSnapshotRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sandboxID: S.String.pipe(T.Label()),
    name: S.optional(S.String),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/sandboxes/{sandboxID}/snapshots",
      code: 200,
    }),
  ),
).annotate({
  identifier: "CreateSandboxSnapshotRequest",
}) as any as S.Schema<CreateSandboxSnapshotRequest>;

/** Full names of the snapshot template including team namespace and tag (e.g. team-slug/my-snapshot:v2) */
export type SnapshotInfoNamesList = Array<string>;
export const SnapshotInfoNamesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<SnapshotInfoNamesList>;

export interface SnapshotInfo {
  /** Identifier of the snapshot template including the tag. Uses namespace/alias when a name was provided (e.g. team-slug/my-snapshot:default), otherwise falls back to the raw template ID (e.g. abc123:default). */
  snapshotID: string;
  /** Full names of the snapshot template including team namespace and tag (e.g. team-slug/my-snapshot:v2) */
  names: SnapshotInfoNamesList;
}
export const SnapshotInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    snapshotID: S.String,
    names: SnapshotInfoNamesList,
  }),
).annotate({ identifier: "SnapshotInfo" }) as any as S.Schema<SnapshotInfo>;

export interface CreateSandboxTimeoutRequest {
  sandboxID: string;
  /** Timeout in seconds from the current time after which the sandbox should expire */
  timeout: number;
}
export const CreateSandboxTimeoutRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sandboxID: S.String.pipe(T.Label()),
    timeout: S.Number,
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/sandboxes/{sandboxID}/timeout",
      code: 200,
    }),
  ),
).annotate({
  identifier: "CreateSandboxTimeoutRequest",
}) as any as S.Schema<CreateSandboxTimeoutRequest>;

export interface CreateSandboxTimeoutResponse {}
export const CreateSandboxTimeoutResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "CreateSandboxTimeoutResponse",
}) as any as S.Schema<CreateSandboxTimeoutResponse>;

export interface DeleteSandboxRequest {
  sandboxID: string;
}
export const DeleteSandboxRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sandboxID: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({ method: "DELETE", uri: "/sandboxes/{sandboxID}", code: 200 }),
  ),
).annotate({
  identifier: "DeleteSandboxRequest",
}) as any as S.Schema<DeleteSandboxRequest>;

export interface DeleteSandboxResponse {}
export const DeleteSandboxResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "DeleteSandboxResponse",
}) as any as S.Schema<DeleteSandboxResponse>;

export interface ForkSandboxRequest {
  sandboxID: string;
  /** Time to live for the new forked sandboxes in seconds. */
  timeout?: number;
  /** Number of forked sandboxes to create. All forks boot from the same snapshot, so the snapshot is captured once regardless of count. Each fork succeeds or fails independently; the outcome of each is reported in its entry of the response list. */
  count?: number;
}
export const ForkSandboxRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sandboxID: S.String.pipe(T.Label()),
    timeout: S.optional(S.Number),
    count: S.optional(S.Number),
  }).pipe(
    T.Http({ method: "POST", uri: "/sandboxes/{sandboxID}/fork", code: 200 }),
  ),
).annotate({
  identifier: "ForkSandboxRequest",
}) as any as S.Schema<ForkSandboxRequest>;

export interface Error {
  /** Error code */
  code: number;
  /** Machine-readable semantic error code. Not a closed set; initial values: sandbox_capacity_unavailable, sandbox_placement_timeout, sandbox_no_compatible_node, sandbox_create_failed, internal_server_error. */
  error_code?: string;
  /** Error */
  message: string;
}
export const Error = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    code: S.Number,
    error_code: S.optional(S.String),
    message: S.String,
  }),
).annotate({ identifier: "Error" }) as any as S.Schema<Error>;

/** Result of one requested fork. Exactly one of sandbox or error is set: sandbox when the fork started successfully, error when it failed to start. */
export interface SandboxForkResult {
  sandbox?: Sandbox;
  error?: Error;
}
export const SandboxForkResult = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sandbox: S.optional(Sandbox),
    error: S.optional(Error),
  }),
).annotate({
  identifier: "SandboxForkResult",
}) as any as S.Schema<SandboxForkResult>;

export type ForkSandboxResponseBodyList = Array<SandboxForkResult>;
export const ForkSandboxResponseBodyList = /*@__PURE__*/ S.Array(
  SandboxForkResult,
) as any as S.Schema<ForkSandboxResponseBodyList>;

export type ForkSandboxResponse = ForkSandboxResponseBodyList;
export const ForkSandboxResponse = /*@__PURE__*/ S.suspend(() =>
  ForkSandboxResponseBodyList.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "ForkSandboxResponse",
}) as any as S.Schema<ForkSandboxResponse>;

export interface GetSandboxRequest {
  sandboxID: string;
}
export const GetSandboxRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sandboxID: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/sandboxes/{sandboxID}", code: 200 })),
).annotate({
  identifier: "GetSandboxRequest",
}) as any as S.Schema<GetSandboxRequest>;

/** State of the sandbox */
export type SandboxState = "running" | "paused";
export const SandboxState = S.String;

/** Action taken when the sandbox times out. */
export type SandboxOnTimeout = "kill" | "pause";
export const SandboxOnTimeout = S.String;

/** Sandbox lifecycle policy returned by sandbox info. */
export interface SandboxLifecycle {
  /** Whether the sandbox can auto-resume. */
  autoResume: boolean;
  onTimeout: SandboxOnTimeout;
}
export const SandboxLifecycle = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    autoResume: S.Boolean,
    onTimeout: SandboxOnTimeout,
  }),
).annotate({
  identifier: "SandboxLifecycle",
}) as any as S.Schema<SandboxLifecycle>;

export type SandboxDetailVolumeMountsList = Array<SandboxVolumeMount>;
export const SandboxDetailVolumeMountsList = /*@__PURE__*/ S.Array(
  SandboxVolumeMount,
) as any as S.Schema<SandboxDetailVolumeMountsList>;

export interface SandboxDetail {
  /** Identifier of the template from which is the sandbox created */
  templateID: string;
  /** Alias of the template */
  alias?: string;
  /** Identifier of the sandbox */
  sandboxID: string;
  /** Identifier of the client */
  clientID: string;
  /** Time when the sandbox was started */
  startedAt: string;
  /** Time when the sandbox will expire */
  endAt: string;
  envdVersion: string;
  /** Access token used for envd communication */
  envdAccessToken?: string;
  /** Whether internet access was explicitly enabled or disabled for the sandbox. Null means it was not explicitly set. */
  allowInternetAccess?: boolean | null;
  /** Base domain where the sandbox traffic is accessible */
  domain?: string | null;
  cpuCount: number;
  memoryMB: number;
  diskSizeMB: number;
  metadata?: SandboxMetadata;
  state: SandboxState;
  network?: SandboxNetworkConfig;
  lifecycle?: SandboxLifecycle;
  volumeMounts?: SandboxDetailVolumeMountsList;
}
export const SandboxDetail = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    templateID: S.String,
    alias: S.optional(S.String),
    sandboxID: S.String,
    clientID: S.String,
    startedAt: S.String,
    endAt: S.String,
    envdVersion: S.String,
    envdAccessToken: S.optional(S.String),
    allowInternetAccess: S.optional(S.NullOr(S.Boolean)),
    domain: S.optional(S.NullOr(S.String)),
    cpuCount: S.Number,
    memoryMB: S.Number,
    diskSizeMB: S.Number,
    metadata: S.optional(SandboxMetadata),
    state: SandboxState,
    network: S.optional(SandboxNetworkConfig),
    lifecycle: S.optional(SandboxLifecycle),
    volumeMounts: S.optional(SandboxDetailVolumeMountsList),
  }),
).annotate({ identifier: "SandboxDetail" }) as any as S.Schema<SandboxDetail>;

export type GetSandboxMetricsRequestSandboxIdsList = Array<string>;
export const GetSandboxMetricsRequestSandboxIdsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<GetSandboxMetricsRequestSandboxIdsList>;

export interface GetSandboxMetricsRequest {
  /** Comma-separated list of sandbox IDs to get metrics for */
  sandbox_ids: GetSandboxMetricsRequestSandboxIdsList;
}
export const GetSandboxMetricsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sandbox_ids: GetSandboxMetricsRequestSandboxIdsList.pipe(
      T.Query(),
      T.QueryJoin(","),
    ),
  }).pipe(T.Http({ method: "GET", uri: "/sandboxes/metrics", code: 200 })),
).annotate({
  identifier: "GetSandboxMetricsRequest",
}) as any as S.Schema<GetSandboxMetricsRequest>;

/** Metric entry with timestamp and line */
export interface SandboxMetric {
  /** Timestamp of the metric entry */
  timestamp: string;
  /** Timestamp of the metric entry in Unix time (seconds since epoch) */
  timestampUnix: number;
  /** Number of CPU cores */
  cpuCount: number;
  /** CPU usage percentage */
  cpuUsedPct: number;
  /** Memory used in bytes */
  memUsed: number;
  /** Total memory in bytes */
  memTotal: number;
  /** Cached memory (page cache) in bytes */
  memCache: number;
  /** Disk used in bytes */
  diskUsed: number;
  /** Total disk space in bytes */
  diskTotal: number;
}
export const SandboxMetric = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    timestamp: S.String,
    timestampUnix: S.Number,
    cpuCount: S.Number,
    cpuUsedPct: S.Number,
    memUsed: S.Number,
    memTotal: S.Number,
    memCache: S.Number,
    diskUsed: S.Number,
    diskTotal: S.Number,
  }),
).annotate({ identifier: "SandboxMetric" }) as any as S.Schema<SandboxMetric>;

export type SandboxesWithMetricsSandboxesMap = {
  [key: string]: SandboxMetric | undefined;
};
export const SandboxesWithMetricsSandboxesMap = /*@__PURE__*/ S.Record(
  S.String,
  SandboxMetric,
) as any as S.Schema<SandboxesWithMetricsSandboxesMap>;

export interface SandboxesWithMetrics {
  sandboxes: SandboxesWithMetricsSandboxesMap;
}
export const SandboxesWithMetrics = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sandboxes: SandboxesWithMetricsSandboxesMap,
  }),
).annotate({
  identifier: "SandboxesWithMetrics",
}) as any as S.Schema<SandboxesWithMetrics>;

export type ListSandboxesRequestStateList = Array<SandboxState | (string & {})>;
export const ListSandboxesRequestStateList = /*@__PURE__*/ S.Array(
  SandboxState,
) as any as S.Schema<ListSandboxesRequestStateList>;

/** Sort direction */
export type OrderDirection = "asc" | "desc";
export const OrderDirection = S.String;

export interface ListSandboxesRequest {
  /** Metadata query used to filter the sandboxes (e.g. "user=abc&app=prod"). Each key and values must be URL encoded. */
  metadata?: string;
  /** Filter sandboxes by one or more states */
  state?: ListSandboxesRequestStateList;
  /** Sort direction by sandbox start time. Defaults to desc (newest first). */
  order?: OrderDirection | (string & {});
  /** Return sandboxes started at or after this timestamp. */
  startedAfter?: string;
  /** Filter sandboxes by a template ID or alias. */
  template?: string;
  /** Cursor to start the list from */
  nextToken?: string;
  /** Maximum number of items to return per page */
  limit?: number;
}
export const ListSandboxesRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    metadata: S.optional(S.String.pipe(T.Query())),
    state: S.optional(
      ListSandboxesRequestStateList.pipe(T.Query(), T.QueryJoin(",")),
    ),
    order: S.optional(OrderDirection.pipe(T.Query())),
    startedAfter: S.optional(S.String.pipe(T.Query())),
    template: S.optional(S.String.pipe(T.Query())),
    nextToken: S.optional(S.String.pipe(T.Query())),
    limit: S.optional(S.Number.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/v2/sandboxes", code: 200 })),
).annotate({
  identifier: "ListSandboxesRequest",
}) as any as S.Schema<ListSandboxesRequest>;

export type ListedSandboxVolumeMountsList = Array<SandboxVolumeMount>;
export const ListedSandboxVolumeMountsList = /*@__PURE__*/ S.Array(
  SandboxVolumeMount,
) as any as S.Schema<ListedSandboxVolumeMountsList>;

export interface ListedSandbox {
  /** Identifier of the template from which is the sandbox created */
  templateID: string;
  /** Alias of the template */
  alias?: string;
  /** Identifier of the sandbox */
  sandboxID: string;
  /** Identifier of the client */
  clientID: string;
  /** Time when the sandbox was started */
  startedAt: string;
  /** Time when the sandbox will expire */
  endAt: string;
  cpuCount: number;
  memoryMB: number;
  diskSizeMB: number;
  metadata?: SandboxMetadata;
  state: SandboxState;
  envdVersion: string;
  volumeMounts?: ListedSandboxVolumeMountsList;
}
export const ListedSandbox = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    templateID: S.String,
    alias: S.optional(S.String),
    sandboxID: S.String,
    clientID: S.String,
    startedAt: S.String,
    endAt: S.String,
    cpuCount: S.Number,
    memoryMB: S.Number,
    diskSizeMB: S.Number,
    metadata: S.optional(SandboxMetadata),
    state: SandboxState,
    envdVersion: S.String,
    volumeMounts: S.optional(ListedSandboxVolumeMountsList),
  }),
).annotate({ identifier: "ListedSandbox" }) as any as S.Schema<ListedSandbox>;

export type ListSandboxesResponseBodyList = Array<ListedSandbox>;
export const ListSandboxesResponseBodyList = /*@__PURE__*/ S.Array(
  ListedSandbox,
) as any as S.Schema<ListSandboxesResponseBodyList>;

export interface ListSandboxesResponse {
  sandboxes: ListSandboxesResponseBodyList;
  nextToken?: string;
  totalRunning?: number;
}
export const ListSandboxesResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sandboxes: ListSandboxesResponseBodyList.pipe(T.RawResponse()),
    nextToken: S.optional(S.String.pipe(T.Header("X-Next-Token"))),
    totalRunning: S.optional(S.Number.pipe(T.Header("X-Total-Running"))),
  }),
).annotate({
  identifier: "ListSandboxesResponse",
}) as any as S.Schema<ListSandboxesResponse>;

/** Direction of the logs that should be returned */
export type LogsDirection = "forward" | "backward";
export const LogsDirection = S.String;

/** State of the sandbox */
export type LogLevel = "debug" | "info" | "warn" | "error";
export const LogLevel = S.String;

export interface ListSandboxLogsRequest {
  sandboxID: string;
  /** Starting timestamp of the logs that should be returned in milliseconds */
  cursor?: number;
  /** Maximum number of logs that should be returned */
  limit?: number;
  /** Direction of the logs that should be returned */
  direction?: LogsDirection | (string & {});
  /** Minimum log level to return. Logs below this level are excluded */
  level?: LogLevel | (string & {});
  /** Case-sensitive substring match on log message content */
  search?: string;
}
export const ListSandboxLogsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sandboxID: S.String.pipe(T.Label()),
    cursor: S.optional(S.Number.pipe(T.Query())),
    limit: S.optional(S.Number.pipe(T.Query())),
    direction: S.optional(LogsDirection.pipe(T.Query())),
    level: S.optional(LogLevel.pipe(T.Query())),
    search: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({ method: "GET", uri: "/v2/sandboxes/{sandboxID}/logs", code: 200 }),
  ),
).annotate({
  identifier: "ListSandboxLogsRequest",
}) as any as S.Schema<ListSandboxLogsRequest>;

export type SandboxLogEntryFieldsMap = { [key: string]: string | undefined };
export const SandboxLogEntryFieldsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<SandboxLogEntryFieldsMap>;

export interface SandboxLogEntry {
  /** Timestamp of the log entry */
  timestamp: string;
  /** Log message content */
  message: string;
  level: LogLevel;
  fields: SandboxLogEntryFieldsMap;
}
export const SandboxLogEntry = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    timestamp: S.String,
    message: S.String,
    level: LogLevel,
    fields: SandboxLogEntryFieldsMap,
  }),
).annotate({
  identifier: "SandboxLogEntry",
}) as any as S.Schema<SandboxLogEntry>;

/** Sandbox logs structured */
export type SandboxLogsV2ResponseLogsList = Array<SandboxLogEntry>;
export const SandboxLogsV2ResponseLogsList = /*@__PURE__*/ S.Array(
  SandboxLogEntry,
) as any as S.Schema<SandboxLogsV2ResponseLogsList>;

export interface SandboxLogsV2Response {
  /** Sandbox logs structured */
  logs: SandboxLogsV2ResponseLogsList;
}
export const SandboxLogsV2Response = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    logs: SandboxLogsV2ResponseLogsList,
  }),
).annotate({
  identifier: "SandboxLogsV2Response",
}) as any as S.Schema<SandboxLogsV2Response>;

export interface ListSandboxMetricsRequest {
  sandboxID: string;
  /** Unix timestamp for the start of the interval, in seconds, for which the metrics */
  start?: number;
  end?: number;
}
export const ListSandboxMetricsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sandboxID: S.String.pipe(T.Label()),
    start: S.optional(S.Number.pipe(T.Query())),
    end: S.optional(S.Number.pipe(T.Query())),
  }).pipe(
    T.Http({ method: "GET", uri: "/sandboxes/{sandboxID}/metrics", code: 200 }),
  ),
).annotate({
  identifier: "ListSandboxMetricsRequest",
}) as any as S.Schema<ListSandboxMetricsRequest>;

export type ListSandboxMetricsResponseBodyList = Array<SandboxMetric>;
export const ListSandboxMetricsResponseBodyList = /*@__PURE__*/ S.Array(
  SandboxMetric,
) as any as S.Schema<ListSandboxMetricsResponseBodyList>;

export type ListSandboxMetricsResponse = ListSandboxMetricsResponseBodyList;
export const ListSandboxMetricsResponse = /*@__PURE__*/ S.suspend(() =>
  ListSandboxMetricsResponseBodyList.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "ListSandboxMetricsResponse",
}) as any as S.Schema<ListSandboxMetricsResponse>;

export interface PauseSandboxRequest {
  sandboxID: string;
  /** Whether to capture a full memory snapshot. When false, only the filesystem is persisted and resuming the sandbox cold-boots (reboots) it from disk, losing in-memory state, running processes, and open connections. Resume it with an explicit request (connect or resume); auto-resume, which can be triggered by arbitrary traffic, refuses such a sandbox. Defaults to true. */
  memory?: boolean;
}
export const PauseSandboxRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sandboxID: S.String.pipe(T.Label()),
    memory: S.optional(S.Boolean),
  }).pipe(
    T.Http({ method: "POST", uri: "/sandboxes/{sandboxID}/pause", code: 200 }),
  ),
).annotate({
  identifier: "PauseSandboxRequest",
}) as any as S.Schema<PauseSandboxRequest>;

export interface PauseSandboxResponse {}
export const PauseSandboxResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "PauseSandboxResponse",
}) as any as S.Schema<PauseSandboxResponse>;

/** List of allowed destinations for egress traffic. Each entry can be a CIDR block (e.g. "8.8.8.8/32"), a bare IP address (e.g. "8.8.8.8"), or a domain name (e.g. "example.com", "*.example.com"). Allowed entries always take precedence over denied entries. */
export type PutSandboxNetworkRequestAllowOutList = Array<string>;
export const PutSandboxNetworkRequestAllowOutList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<PutSandboxNetworkRequestAllowOutList>;

/** List of denied CIDR blocks or IP addresses for egress traffic. Domain names are not supported for deny rules. */
export type PutSandboxNetworkRequestDenyOutList = Array<string>;
export const PutSandboxNetworkRequestDenyOutList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<PutSandboxNetworkRequestDenyOutList>;

export type PutSandboxNetworkRequestRulesValueList = Array<SandboxNetworkRule>;
export const PutSandboxNetworkRequestRulesValueList = /*@__PURE__*/ S.Array(
  SandboxNetworkRule,
) as any as S.Schema<PutSandboxNetworkRequestRulesValueList>;

/** Per-domain transform rules applied to matching outbound HTTPS requests. Replaces all existing rules when provided. Keys may be exact DNS names or a single leading wildcard (for example, "*.example.com"), and are normalized to lowercase on write. Wildcards match subdomains at any depth but not the apex domain; a bare "*" is invalid. Exact rules take precedence, followed by the longest matching wildcard suffix, and matching rule sets are not merged. Broad wildcards such as "*.com" are allowed and may expose transformed credentials to every matching destination the sandbox contacts. Rules do not grant network access; configure allowOut separately to permit the destination. */
export type PutSandboxNetworkRequestRulesMap = {
  [key: string]: PutSandboxNetworkRequestRulesValueList | undefined;
};
export const PutSandboxNetworkRequestRulesMap = /*@__PURE__*/ S.Record(
  S.String,
  PutSandboxNetworkRequestRulesValueList,
) as any as S.Schema<PutSandboxNetworkRequestRulesMap>;

export interface PutSandboxNetworkRequest {
  sandboxID: string;
  /** List of allowed destinations for egress traffic. Each entry can be a CIDR block (e.g. "8.8.8.8/32"), a bare IP address (e.g. "8.8.8.8"), or a domain name (e.g. "example.com", "*.example.com"). Allowed entries always take precedence over denied entries. */
  allowOut?: PutSandboxNetworkRequestAllowOutList;
  /** List of denied CIDR blocks or IP addresses for egress traffic. Domain names are not supported for deny rules. */
  denyOut?: PutSandboxNetworkRequestDenyOutList;
  egressProxy?: SandboxEgressProxyConfig | null;
  /** Per-domain transform rules applied to matching outbound HTTPS requests. Replaces all existing rules when provided. Keys may be exact DNS names or a single leading wildcard (for example, "*.example.com"), and are normalized to lowercase on write. Wildcards match subdomains at any depth but not the apex domain; a bare "*" is invalid. Exact rules take precedence, followed by the longest matching wildcard suffix, and matching rule sets are not merged. Broad wildcards such as "*.com" are allowed and may expose transformed credentials to every matching destination the sandbox contacts. Rules do not grant network access; configure allowOut separately to permit the destination. */
  rules?: PutSandboxNetworkRequestRulesMap;
  /** Allow sandbox to access the internet. When set to false, it behaves the same as specifying denyOut to 0.0.0.0/0 in the network config. */
  allow_internet_access?: boolean;
}
export const PutSandboxNetworkRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sandboxID: S.String.pipe(T.Label()),
    allowOut: S.optional(PutSandboxNetworkRequestAllowOutList),
    denyOut: S.optional(PutSandboxNetworkRequestDenyOutList),
    egressProxy: S.optional(S.NullOr(SandboxEgressProxyConfig)),
    rules: S.optional(PutSandboxNetworkRequestRulesMap),
    allow_internet_access: S.optional(S.Boolean),
  }).pipe(
    T.Http({ method: "PUT", uri: "/sandboxes/{sandboxID}/network", code: 200 }),
  ),
).annotate({
  identifier: "PutSandboxNetworkRequest",
}) as any as S.Schema<PutSandboxNetworkRequest>;

export interface PutSandboxNetworkResponse {}
export const PutSandboxNetworkResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "PutSandboxNetworkResponse",
}) as any as S.Schema<PutSandboxNetworkResponse>;

export type ConnectSandboxError = E2BOpError;
/** Connect sandbox Returns sandbox details. If the sandbox is paused, it will be resumed. TTL is only extended. */
export const connectSandbox: API.OperationMethod<
  ConnectSandboxRequest,
  Sandbox,
  ConnectSandboxError,
  E2BOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ConnectSandboxRequest,
  output: Sandbox,
  errors: [UnknownE2BError],
  protocol: E2BProtocol,
  retry: Retry.Retry,
}));

export type CreateSandboxError = E2BOpError;
/** Create sandbox Create a sandbox from the template */
export const createSandbox: API.OperationMethod<
  CreateSandboxRequest,
  Sandbox,
  CreateSandboxError,
  E2BOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateSandboxRequest,
  output: Sandbox,
  errors: [UnknownE2BError],
  protocol: E2BProtocol,
  retry: Retry.Retry,
}));

export type CreateSandboxRefreshError = E2BOpError;
/** Refresh sandbox Refresh the sandbox extending its time to live */
export const createSandboxRefresh: API.OperationMethod<
  CreateSandboxRefreshRequest,
  CreateSandboxRefreshResponse,
  CreateSandboxRefreshError,
  E2BOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateSandboxRefreshRequest,
  output: CreateSandboxRefreshResponse,
  errors: [UnknownE2BError],
  protocol: E2BProtocol,
  retry: Retry.Retry,
}));

export type CreateSandboxSnapshotError = E2BOpError;
/** Create snapshot Create a persistent snapshot from the sandbox's current state. Snapshots can be used to create new sandboxes and persist beyond the original sandbox's lifetime. */
export const createSandboxSnapshot: API.OperationMethod<
  CreateSandboxSnapshotRequest,
  SnapshotInfo,
  CreateSandboxSnapshotError,
  E2BOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateSandboxSnapshotRequest,
  output: SnapshotInfo,
  errors: [UnknownE2BError],
  protocol: E2BProtocol,
  retry: Retry.Retry,
}));

export type CreateSandboxTimeoutError = E2BOpError;
/** Set sandbox timeout Set the timeout for the sandbox. The sandbox will expire x seconds from the time of the request. Calling this method multiple times overwrites the TTL, each time using the current timestamp as the starting point to measure the timeout duration. */
export const createSandboxTimeout: API.OperationMethod<
  CreateSandboxTimeoutRequest,
  CreateSandboxTimeoutResponse,
  CreateSandboxTimeoutError,
  E2BOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateSandboxTimeoutRequest,
  output: CreateSandboxTimeoutResponse,
  errors: [UnknownE2BError],
  protocol: E2BProtocol,
  retry: Retry.Retry,
}));

export type DeleteSandboxError = E2BOpError;
/** Kill sandbox Kill a sandbox */
export const deleteSandbox: API.OperationMethod<
  DeleteSandboxRequest,
  DeleteSandboxResponse,
  DeleteSandboxError,
  E2BOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteSandboxRequest,
  output: DeleteSandboxResponse,
  errors: [UnknownE2BError],
  protocol: E2BProtocol,
  retry: Retry.Retry,
}));

export type ForkSandboxError = E2BOpError;
/** Fork sandbox Fork the sandbox: checkpoint the running sandbox in place (it is briefly paused, snapshotted with its full memory state, and resumed on its node, keeping its ID and expiration untouched) and create count new sandboxes from that snapshot. Returns one result per requested fork, each carrying either the created sandbox or the error that prevented it from starting. A non-201 status means the request failed before any fork was attempted. */
export const forkSandbox: API.OperationMethod<
  ForkSandboxRequest,
  ForkSandboxResponse,
  ForkSandboxError,
  E2BOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ForkSandboxRequest,
  output: ForkSandboxResponse,
  errors: [UnknownE2BError],
  protocol: E2BProtocol,
  retry: Retry.Retry,
}));

export type GetSandboxError = E2BOpError;
/** Sandbox Get a sandbox by id */
export const getSandbox: API.OperationMethod<
  GetSandboxRequest,
  SandboxDetail,
  GetSandboxError,
  E2BOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetSandboxRequest,
  output: SandboxDetail,
  errors: [UnknownE2BError],
  protocol: E2BProtocol,
  retry: Retry.Retry,
}));

export type GetSandboxMetricsError = E2BOpError;
/** List sandbox metrics List metrics for given sandboxes */
export const getSandboxMetrics: API.OperationMethod<
  GetSandboxMetricsRequest,
  SandboxesWithMetrics,
  GetSandboxMetricsError,
  E2BOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetSandboxMetricsRequest,
  output: SandboxesWithMetrics,
  errors: [UnknownE2BError],
  protocol: E2BProtocol,
  retry: Retry.Retry,
}));

export type ListSandboxesError = E2BOpError;
/** List sandboxes (v2) List all sandboxes */
export const listSandboxes: API.PaginatedOperationMethod<
  ListSandboxesRequest,
  ListSandboxesResponse,
  ListSandboxesError,
  E2BOpContext,
  ListedSandbox
> = /*@__PURE__*/ API.makePaginated(() => ({
  input: ListSandboxesRequest,
  output: ListSandboxesResponse,
  errors: [UnknownE2BError],
  protocol: E2BProtocol,
  retry: Retry.Retry,
  pagination: {
    mode: "token",
    inputToken: "nextToken",
    outputToken: "nextToken",
    items: "sandboxes",
  } as const,
})) as any;

export type ListSandboxLogsError = E2BOpError;
/** Sandbox logs (v2) Get sandbox logs */
export const listSandboxLogs: API.OperationMethod<
  ListSandboxLogsRequest,
  SandboxLogsV2Response,
  ListSandboxLogsError,
  E2BOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListSandboxLogsRequest,
  output: SandboxLogsV2Response,
  errors: [UnknownE2BError],
  protocol: E2BProtocol,
  retry: Retry.Retry,
}));

export type ListSandboxMetricsError = E2BOpError;
/** Sandbox metrics Get sandbox metrics */
export const listSandboxMetrics: API.OperationMethod<
  ListSandboxMetricsRequest,
  ListSandboxMetricsResponse,
  ListSandboxMetricsError,
  E2BOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListSandboxMetricsRequest,
  output: ListSandboxMetricsResponse,
  errors: [UnknownE2BError],
  protocol: E2BProtocol,
  retry: Retry.Retry,
}));

export type PauseSandboxError = E2BOpError;
/** Pause sandbox Pause the sandbox */
export const pauseSandbox: API.OperationMethod<
  PauseSandboxRequest,
  PauseSandboxResponse,
  PauseSandboxError,
  E2BOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PauseSandboxRequest,
  output: PauseSandboxResponse,
  errors: [UnknownE2BError],
  protocol: E2BProtocol,
  retry: Retry.Retry,
}));

export type PutSandboxNetworkError = E2BOpError;
/** Update sandbox network Update the network configuration for a running sandbox. Replaces the current egress rules with the provided configuration. Omitting field clears it. */
export const putSandboxNetwork: API.OperationMethod<
  PutSandboxNetworkRequest,
  PutSandboxNetworkResponse,
  PutSandboxNetworkError,
  E2BOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PutSandboxNetworkRequest,
  output: PutSandboxNetworkResponse,
  errors: [UnknownE2BError],
  protocol: E2BProtocol,
  retry: Retry.Retry,
}));
